{
  "name": "playwright_automation_course",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "regression": "npx playwright test",
    "webTests": "npx playwright test --grep @Web",
    "APITests": "npx playwright test --grep @API",
    "newConfig": "npx playwright test --config playwright.config1.js --project=chromium"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.31.2",
    "allure-playwright": "^2.1.0"
  },
  "dependencies": {
    "jest": "^29.5.0",
    "jest-image-snapshot": "^6.1.0",
    "playwright": "^1.31.2"
  }
}
